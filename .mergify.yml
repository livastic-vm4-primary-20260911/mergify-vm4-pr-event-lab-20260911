merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: vm4-policy-revocation
    queue_conditions: true
    merge_conditions:
      - check-success = vm4-policy-revoked-never
    checks_timeout: null
