merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: pr-event-security-probe
    queue_conditions: true
    merge_conditions:
      - check-success = vm4-never-merge-pr-event-guard
    checks_timeout: null
pull_request_rules:
  - name: Autoqueue VM4-B external fork probe
    conditions:
      - author = livastic-vm4-secondary-20260911
      - base = main
    actions:
      queue:
        name: pr-event-security-probe
